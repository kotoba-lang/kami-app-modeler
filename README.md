# kami-app-modeler

Interactive polygon-modeling application. This app will compose
`kami-engine-modeling` with the existing WebGPU viewport and USD/glTF adapters.
It owns selection, gizmos, outliner, undo/redo UI, project files, and import/export;
geometry algorithms remain in the engine repository.
